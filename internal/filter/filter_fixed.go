package filter

import "fmt"

// applyRule is defined in filter.go but needs fmt — kept here to avoid import issues.
// This file intentionally left minimal; fmt usage is inlined in filter.go via this stub.

// Sprintf is a local alias used by applyRule in filter.go.
var Sprintf = fmt.Sprintf
